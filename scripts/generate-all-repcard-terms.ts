import dotenv from 'dotenv';
import path from 'path';
dotenv.config({ path: path.join(process.cwd(), '.env.local') });

/**
 * Complete RepCard Terms Data Generator
 * This file contains all remaining terms from the RepCard API
 */

const remainingTerms = [
  // プロンプトファイルから取得した残りの用語を分類して追加
  // ただし、ファイルが大きすぎるため、直接JavaScriptオブジェクトとして生成
];


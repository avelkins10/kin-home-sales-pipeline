# ⚠️ CRITICAL SECURITY NOTICE ⚠️
# This is a template file. You MUST create your own .env.local file with actual credentials.
# NEVER commit .env.local to version control - it contains sensitive information!

# INSTRUCTIONS:
# 1. Copy this file to .env.local: cp env.example .env.local
# 2. Replace ALL placeholder values with your actual credentials
# 3. Generate a fresh Quickbase token from your Quickbase account
# 4. Get your Neon database URL from your Neon dashboard
# 5. Generate a secure NEXTAUTH_SECRET (use: openssl rand -base64 32)
# 6. (Optional) Configure email provider for invite emails - see Email Configuration section

# Quickbase Configuration
QUICKBASE_REALM=kin.quickbase.com
QUICKBASE_TOKEN=YOUR_QUICKBASE_TOKEN_HERE
QUICKBASE_APP_ID=br9kwm8bk
QUICKBASE_TABLE_PROJECTS=br9kwm8na
QUICKBASE_TABLE_TASKS=br9kwm8q9
QUICKBASE_TABLE_ADDERS=bsaycczmf

# Client-side Quickbase (safe metadata only; DO NOT expose tokens)
NEXT_PUBLIC_QUICKBASE_REALM=kin.quickbase.com
NEXT_PUBLIC_QUICKBASE_TABLE_PROJECTS=br9kwm8na

# Neon Database
DATABASE_URL=YOUR_NEON_DATABASE_URL_HERE
POSTGRES_URL=YOUR_NEON_DATABASE_URL_HERE

# NextAuth Configuration
NEXTAUTH_SECRET=YOUR_NEXTAUTH_SECRET_HERE
NEXTAUTH_URL=http://localhost:3000

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Email Configuration (Optional - for invite emails)
# Choose provider: 'resend' (recommended) or 'sendgrid'
# Get API key from https://resend.com or https://sendgrid.com
# Leave EMAIL_ENABLED=false to disable email sending (invites will still work)
EMAIL_ENABLED=false
MAIL_PROVIDER=resend
RESEND_API_KEY=
SENDGRID_API_KEY=
MAIL_FROM=Kin Home Sales <no-reply@kinhome.com>

# Internal API Secret (for server-to-server communication)
INTERNAL_API_SECRET=your-random-secret-key-here-change-in-development

# Test Credentials (for production smoke tests)
# Use a real test account with closer role permissions
TEST_USER_EMAIL=test@kinhome.com
TEST_USER_PASSWORD=your-test-password-here

# Sentry (Optional - for error tracking)
# Get DSN from https://sentry.io/settings/projects/
# Leave empty to disable Sentry (app will work without it)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=